#include<bits/stdc++.h>
using namespace std;
#define nl '\n'

void solve(){
    string s;
    cin >> s;

    string w = "WUB";

    for (int i = 0; i < s.size(): i++){
        if (s.substr(i, 3) == w){
            if (i > 0 && s[i-1])
        }

    }


}

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    int t = 1;
    //cin >> t;
    while(t--){
        solve();
    }
}